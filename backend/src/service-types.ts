export interface VoiceResult { fileName: string; model: string; aiGenerated: boolean; disclosure: string; audioUrl?: string; }
export interface AvatarResult { provider: string; audioUrl: string; referencePhoto?: string; aiGenerated: boolean; disclosure: string; }
